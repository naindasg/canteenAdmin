<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.canteenadmin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CanteenAdmin">
        <activity android:name=".OrderHistoryDetailActivity"></activity>
        <activity android:name=".OrderHistoryNames" />
        <activity android:name=".EmailActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".OrderDetailActivity" />
        <activity android:name=".UpdateActivity" />
        <activity android:name=".AddFoodActivity" />
        <activity android:name=".CustomerNamesActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>